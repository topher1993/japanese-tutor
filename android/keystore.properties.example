# Copy to android/keystore.properties (ignored by git) for a local signed
# production build. In CI, prefer the equivalent JAPANESE_TUTOR_UPLOAD_*
# environment variables or Gradle properties.
storeFile=app/upload-keystore.jks
storePassword=replace-with-a-secret
keyAlias=upload
keyPassword=replace-with-a-secret
