rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Koi data is server-mediated. Firebase Admin SDK access is not evaluated
    // by these rules; every mobile-client read and write fails closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

