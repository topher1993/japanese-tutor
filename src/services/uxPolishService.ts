export function getPolishedDesignTokens() { return { colors: { background: '#F3F6F5', surface: '#FFFFFF', text: '#172126', muted: '#64727A', primary: '#256D85', safety: '#D95F43', success: '#2A9D8F', border: '#DDE5E2' }, radius: { sm: 12, md: 18, lg: 24, pill: 9999 }, shadow: { card: 'soft-lift', hero: 'calm-depth' }, touchTarget: { minimum: 44 }, typography: { hero: 30, title: 22, body: 16, caption: 13 } }; }
export function getComponentPolishSpec(name: 'LessonHeroCard' | 'SurvivalTopicCard' | 'EmergencyPhraseCard' | 'ProgressStatCard') { const specs = { LessonHeroCard: { borderRadius: 28, surface: 'white elevated card', mobileRule: 'single column with large primary action', emphasis: 'daily lesson continuation' }, SurvivalTopicCard: { borderRadius: 24, surface: 'white card with safety accent when needed', mobileRule: 'single column topic list with 44px minimum touch target', emphasis: 'fast scanning' }, EmergencyPhraseCard: { borderRadius: 24, surface: 'warm safety surface', mobileRule: 'single column with Japanese large first', emphasis: 'urgent readability' }, ProgressStatCard: { borderRadius: 20, surface: 'compact white card', mobileRule: 'single column stats on small screens', emphasis: 'motivation' } } as const; return specs[name]; }
